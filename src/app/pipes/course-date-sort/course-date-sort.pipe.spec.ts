import { CourseDateSortPipe } from './course-date-sort.pipe';

describe('CourseDateSortPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseDateSortPipe();
    expect(pipe).toBeTruthy();
  });
});
